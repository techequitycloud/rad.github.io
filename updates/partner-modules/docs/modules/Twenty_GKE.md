# Twenty CRM on GKE Autopilot

This document provides a comprehensive reference for the `modules/Twenty_GKE` Terraform module. It covers architecture, IAM, configuration variables, Twenty-specific behaviours, and operational patterns for deploying Twenty CRM on Google Kubernetes Engine Autopilot.

---

## 1. Module Overview

Twenty is an open-source CRM with 25,000+ GitHub stars, built as a modern, developer-friendly alternative to Salesforce and HubSpot. `Twenty GKE` is a **wrapper module** built on top of `App GKE`. It uses `App GKE` for all GCP and Kubernetes infrastructure provisioning and injects Twenty-specific application configuration, database initialisation, and secret management via `Twenty Common`.

**Key Capabilities:**
*   **Compute**: GKE Autopilot, Kubernetes Deployment or StatefulSet. 1 vCPU / 1 Gi by default; 2 vCPU / 2 Gi recommended for production.
*   **Data Persistence**: Cloud SQL **PostgreSQL 15** via Cloud SQL Auth Proxy sidecar. Twenty requires PostgreSQL.
*   **Security**: `APP_SECRET` auto-generated by `Twenty Common` and stored in Secret Manager. Accessed at runtime via Workload Identity. Inherits Cloud Armor WAF, IAP, Binary Authorization, and VPC Service Controls from `App GKE`.
*   **Background Jobs**: Uses **pg-boss** (PostgreSQL-backed job queue) by default. When `enable_redis = true`, switches to **bull-mq** using the provided Redis instance.
*   **File Storage**: When `enable_gcs_storage = true`, a GCS bucket is provisioned and Twenty is configured to use the S3-compatible GCS API (`STORAGE_TYPE=s3`).
*   **Workload Identity**: GKE pods access Secret Manager via Workload Identity Federation — no service account keys are mounted.
*   **CI/CD**: Cloud Build custom image pipeline by default; Cloud Deploy progressive delivery optional.

**Project & Application Identity**

| Variable | Group | Type | Default | Description |
|---|---|---|---|---|
| `project_id` | 1 | `string` | — | GCP project ID. **Required.** |
| `tenant_deployment_id` | 2 | `string` | `'demo'` | Short suffix appended to all resource names. |
| `support_users` | 2 | `list(string)` | `[]` | Email recipients for monitoring alerts. |
| `resource_labels` | 2 | `map(string)` | `{}` | Labels applied to all provisioned resources. |
| `application_name` | 3 | `string` | `'twenty'` | Base resource name. Do not change after initial deployment. |
| `application_display_name` | 3 | `string` | `'Twenty CRM'` | Human-readable name shown in monitoring dashboards. |
| `application_description` | 3 | `string` | `'Twenty CRM on GKE Autopilot'` | Pod/deployment description. |
| `application_version` | 3 | `string` | `'latest'` | Twenty image version tag. Pin to a specific version for production. |

**Wrapper architecture:** `Twenty GKE` calls `Twenty Common` to build an `application_config` object containing Twenty-specific environment variables, probe configuration, and the default `db-init` job definition. `Twenty Common` generates `APP_SECRET` and stores it in Secret Manager. The GKE pod accesses this secret at runtime via Workload Identity without explicit secret mounting. `module_storage_buckets` carries the GCS storage bucket when `enable_gcs_storage = true`.

---

## 2. IAM & Access Control

`Twenty_GKE` delegates all IAM provisioning to `App_GKE`. The GKE SA, Cloud Build SA, Workload Identity bindings, and IAP service agent role sets are identical to those in [App_GKE §2](../App_GKE/App_GKE.md#2-iam--access-control).

**Workload Identity:** GKE pods access Secret Manager secrets (including `APP_SECRET`) through Workload Identity Federation — the Kubernetes SA is bound to a GCP SA that has `secretmanager.secretAccessor` on the module-managed secrets. No SA keys or credential files are mounted.

**APP_SECRET auto-generation:** `Twenty Common` generates a random 32-character alphanumeric `APP_SECRET` at apply time. Stored in Secret Manager as `<resource_prefix>-app-secret`. The plaintext value is never stored in Terraform state.

**Database initialisation identity:** The `db-init` Kubernetes Job runs under the GKE SA with Workload Identity. It connects to Cloud SQL PostgreSQL via the Auth Proxy sidecar using `ROOT_PASSWORD` from Secret Manager to create the application database and user.

For the complete role tables and IAP, password rotation, and public access details, see [App_GKE §2](../App_GKE/App_GKE.md#2-iam--access-control).

---

## 3. Core Service Configuration

### A. Compute (GKE)

Twenty on GKE runs as a Kubernetes Deployment with a Cloud SQL Auth Proxy sidecar. GKE Autopilot automatically provisions nodes matching the pod's resource requests.

**Container image:** `container_image_source` defaults to `'custom'`, meaning Cloud Build compiles a custom image using `Twenty_Common`'s Dockerfile. Set `container_image_source = 'prebuilt'` and `container_image = 'twentycrm/twenty:0.50.0'` to deploy the upstream image directly.

| Variable | Group | Default | Description |
|---|---|---|---|
| `deploy_application` | 4 | `true` | Set `false` for infrastructure-only deployment. |
| `container_image_source` | 4 | `'custom'` | `'custom'` (Cloud Build) or `'prebuilt'` (existing image URI). |
| `container_image` | 4 | `""` | Override image URI. Leave empty for Cloud Build to manage. |
| `container_build_config` | 4 | `{ enabled=true }` | Cloud Build configuration. |
| `container_resources` | 4 | `{ cpu_limit="1000m", memory_limit="1Gi" }` | Pod CPU/memory limits and optional requests. |
| `min_instance_count` | 4 | `1` | Minimum pod replicas. |
| `max_instance_count` | 4 | `3` | Maximum pod replicas. |
| `container_port` | 4 | `3000` | Twenty's native HTTP port. |
| `timeout_seconds` | 4 | `300` | Maximum backend pod response time in seconds. |
| `container_protocol` | 4 | `'http1'` | `'http1'` or `'h2c'`. |
| `enable_cloudsql_volume` | 4 | `true` | Injects Cloud SQL Auth Proxy sidecar into the pod. |
| `cloudsql_volume_mount_path` | 4 | `'/cloudsql'` | Container path for the Auth Proxy Unix socket. |
| `enable_image_mirroring` | 4 | `true` | Mirrors the Twenty image into Artifact Registry. |
| `enable_vertical_pod_autoscaling` | 4 | `false` | Enables VPA for automatic resource adjustment. Recommended for Autopilot. |
| `service_annotations` | 4 | `{}` | Custom annotations applied to the Kubernetes Service. |
| `service_labels` | 4 | `{}` | Custom labels applied to the Kubernetes Service. |
| `deployment_timeout` | 6 | `1800` | Seconds Terraform waits for the Deployment rollout to complete. |

### B. Kubernetes Workload Configuration

| Variable | Group | Default | Description |
|---|---|---|---|
| `workload_type` | 6 | `null` | `'Deployment'` (stateless) or `'StatefulSet'` (stateful). Auto-selects `StatefulSet` when `stateful_pvc_enabled = true`. |
| `service_type` | 6 | `'LoadBalancer'` | Kubernetes Service type: `'ClusterIP'`, `'LoadBalancer'`, or `'NodePort'`. |
| `session_affinity` | 6 | `'None'` | `'None'` (round-robin) or `'ClientIP'` (sticky sessions). |
| `namespace_name` | 6 | `""` | Kubernetes namespace. Auto-generated if empty. |
| `gke_cluster_name` | 6 | `""` | Target GKE cluster name. Auto-discovered if empty. |
| `gke_cluster_selection_mode` | 6 | `'primary'` | Cluster selection: `'primary'`, `'round-robin'`, or `'explicit'`. |
| `prereq_gke_subnet_cidr` | 6 | `'10.201.0.0/24'` | CIDR for inline GKE subnet when no cluster exists. Must not overlap other subnets. |
| `network_name` | 19 | `""` | VPC network name. Auto-discovered if empty. |
| `network_tags` | 19 | `[]` | Network tags applied to GKE nodes and pods. |
| `termination_grace_period_seconds` | 6 | `30` | Seconds Kubernetes waits after SIGTERM before forcibly terminating. |
| `enable_network_segmentation` | 6 | `false` | Creates Kubernetes NetworkPolicy resources. |
| `configure_service_mesh` | 6 | `false` | Enables Istio service mesh injection. |
| `enable_multi_cluster_service` | 6 | `false` | Creates a ServiceExport for Multi-Cluster Services. |

### C. Database (Cloud SQL — PostgreSQL 15)

Twenty requires **PostgreSQL 15**. The Cloud SQL Auth Proxy sidecar handles the connection via Unix socket.

| Variable | Group | Default | Description |
|---|---|---|---|
| `database_type` | 15 | `'POSTGRES_15'` | Cloud SQL engine. Options: `POSTGRES_15`, `POSTGRES_14`, `POSTGRES_13`. Must be PostgreSQL. |
| `application_database_name` | 15 | `'twenty'` | PostgreSQL database name. **Do not change after initial deployment.** |
| `application_database_user` | 15 | `'twenty'` | PostgreSQL application user. Password auto-generated and stored in Secret Manager. |
| `database_password_length` | 15 | `32` | Auto-generated password length. Range: 16–64. |
| `enable_postgres_extensions` | 15 | `false` | Enables installation of additional PostgreSQL extensions during `db-init`. |
| `postgres_extensions` | 15 | `[]` | List of extensions to install (e.g., `['pgvector', 'pg_trgm']`). |
| `sql_instance_name` | 15 | `""` | Name of an existing Cloud SQL instance. Auto-discovered if empty. |
| `sql_instance_base_name` | 15 | `""` | Base name for a new Cloud SQL instance. |
| `enable_auto_password_rotation` | 5 | `false` | Automated zero-downtime password rotation. |
| `rotation_propagation_delay_sec` | 5 | `90` | Seconds to wait after rotation before restarting pods. |

### D. Storage (GCS)

**GCS storage is disabled by default** (`enable_gcs_storage = false`). When disabled, Twenty uses local container storage for file attachments — ephemeral and lost on pod restart.

| Variable | Group | Default | Description |
|---|---|---|---|
| `enable_gcs_storage` | 11 | `false` | Provisions a GCS bucket for persistent file storage using the S3-compatible API. |
| `create_cloud_storage` | 11 | `true` | Controls whether additional `storage_buckets` entries are provisioned. |
| `storage_buckets` | 11 | `[]` | Additional GCS buckets to provision. |
| `gcs_volumes` | 11 | `[]` | GCS buckets to mount via GCS Fuse. |
| `manage_storage_kms_iam` | 11 | `false` | Creates a CMEK KMS keyring/key. |
| `enable_artifact_registry_cmek` | 11 | `false` | Creates an Artifact Registry KMS key. |
| `max_images_to_retain` | 11 | `7` | Maximum number of recent container images to keep. |
| `delete_untagged_images` | 11 | `true` | Automatically deletes untagged images from Artifact Registry. |
| `image_retention_days` | 11 | `30` | Days after which images are eligible for deletion. |

### E. Stateful Workloads (PVC)

When `stateful_pvc_enabled = true`, a PersistentVolumeClaim is created and `workload_type` automatically resolves to `StatefulSet`. This is useful for running Twenty with persistent local storage (though GCS storage is preferred for production).

> **Important:** Setting `stateful_pvc_enabled = true` alongside `workload_type = "Deployment"` fails at plan time. Setting `stateful_pvc_enabled = true` without `workload_type` automatically selects `StatefulSet`.

| Variable | Group | Default | Description |
|---|---|---|---|
| `stateful_pvc_enabled` | 16 | `false` | Creates a PVC and auto-selects StatefulSet. |
| `stateful_pvc_size` | 16 | `'10Gi'` | PVC size. |
| `stateful_pvc_mount_path` | 16 | `'/data'` | Container path where PVC is mounted. |
| `stateful_pvc_storage_class` | 16 | `""` | Kubernetes StorageClass. Cluster default if empty. |
| `stateful_headless_service` | 16 | `false` | Creates a headless service for StatefulSet pod DNS. |
| `stateful_pod_management_policy` | 16 | `'OrderedReady'` | `'OrderedReady'` or `'Parallel'`. |
| `stateful_update_strategy` | 16 | `'RollingUpdate'` | `'RollingUpdate'` or `'OnDelete'`. |
| `stateful_fs_group` | 16 | `0` | GID for fsGroup in the pod security context. Set to the Twenty process GID if write access to the PVC is needed. |

### F. Networking & Access

| Variable | Group | Default | Description |
|---|---|---|---|
| `enable_custom_domain` | 19 | `false` | Provisions a Kubernetes Ingress for custom hostnames. |
| `application_domains` | 19 | `[]` | Custom domain names for the Ingress. DNS must point to the load balancer IP. |
| `enable_cdn` | 19 | `false` | Enables Cloud CDN on the Global HTTPS Load Balancer. Requires `enable_custom_domain = true`. |
| `reserve_static_ip` | 19 | `true` | Provisions a global static external IP. Recommended for production. |
| `static_ip_name` | 19 | `""` | Name for the reserved static IP. Auto-generated if empty. |

### G. Initialization & Bootstrap

A `db-init` Kubernetes Job is automatically provisioned by `Twenty Common` when `initialization_jobs` is left as the default empty list. It creates the PostgreSQL database and user using `ROOT_PASSWORD` from Secret Manager.

| Variable | Group | Default | Description |
|---|---|---|---|
| `initialization_jobs` | 13 | `[]` | One-shot Kubernetes Jobs. Leave empty for `Twenty Common` to supply the default `db-init` job. Non-empty list replaces it. |
| `cron_jobs` | 13 | `[]` | Recurring Kubernetes CronJobs. Each entry: `name`, `schedule`, `image`, `command`, `args`, `env_vars`, `secret_env_vars`, `cpu_limit`, `memory_limit`, `timeout_seconds`, `max_retries`, `task_count`, `parallelism`, `mount_nfs`, `mount_gcs_volumes`, `script_path`, `paused`. |
| `additional_services` | 6 | `[]` | Additional Kubernetes Deployments alongside the main app (e.g., dedicated worker for bull-mq). |

---

## 4. Advanced Security

### A. Cloud Armor WAF

When `enable_cloud_armor = true`, a Cloud Armor security policy is attached to the GKE Ingress backend.

| Variable | Group | Default | Description |
|---|---|---|---|
| `enable_cloud_armor` | 21 | `false` | Attaches a Cloud Armor policy to the Ingress backend. |
| `admin_ip_ranges` | 21 | `[]` | CIDR ranges permitted for privileged admin access. |
| `cloud_armor_policy_name` | 21 | `'default-waf-policy'` | Name of the Cloud Armor policy to attach. |

### B. Identity-Aware Proxy (IAP)

When `enable_iap = true`, IAP is enabled on the GKE backend service. Unlike Cloud Run's native IAP, the GKE variant requires OAuth client credentials.

| Variable | Group | Default | Description |
|---|---|---|---|
| `enable_iap` | 20 | `false` | Enables IAP on the GKE backend service. |
| `iap_authorized_users` | 20 | `[]` | Users granted IAP access. |
| `iap_authorized_groups` | 20 | `[]` | Google Groups granted IAP access. |
| `iap_oauth_client_id` | 20 | `""` | OAuth 2.0 Client ID. Required when `enable_iap = true`. Sensitive. |
| `iap_oauth_client_secret` | 20 | `""` | OAuth 2.0 Client Secret. Required when `enable_iap = true`. Sensitive. |
| `iap_support_email` | 20 | `""` | Support email displayed on the OAuth consent screen. |

### C. Binary Authorization

| Variable | Group | Default | Description |
|---|---|---|---|
| `enable_binary_authorization` | 12 | `false` | Enforces image attestation on GKE workloads. |
| `binauthz_evaluation_mode` | 12 | `'ALWAYS_ALLOW'` | Enforcement mode: `'ALWAYS_ALLOW'`, `'REQUIRE_ATTESTATION'`, or `'ALWAYS_DENY'`. |

### D. VPC Service Controls

| Variable | Group | Default | Description |
|---|---|---|---|
| `enable_vpc_sc` | 22 | `false` | Registers module API calls within the project's VPC-SC perimeter. |
| `vpc_sc_dry_run` | 22 | `true` | Logs VPC-SC violations without blocking. |
| `organization_id` | 22 | `""` | GCP Organization ID for VPC-SC. **Must be set explicitly.** |

### E. Secret Manager

Twenty secrets managed by Secret Manager and injected via Workload Identity:

| Variable | Group | Default | Description |
|---|---|---|---|
| `secret_environment_variables` | 5 | `{}` | Additional Secret Manager references (e.g., `{ STORAGE_S3_ACCESS_KEY_ID = "twenty-s3-key" }`). |
| `secret_rotation_period` | 5 | `'2592000s'` | Rotation notification frequency. Default: 30 days. |
| `secret_propagation_delay` | 5 | `30` | Seconds to wait after secret creation. |

---

## 5. Reliability

### A. Resource Quotas

ResourceQuotas can be enforced at the namespace level.

> **Important:** `quota_memory_requests` and `quota_memory_limits` must use binary unit suffixes (e.g., `'4Gi'`, `'8192Mi'`). Bare integers are treated as bytes by Kubernetes and block all pod scheduling.

| Variable | Group | Default | Description |
|---|---|---|---|
| `enable_resource_quota` | 8 | `false` | Creates a Kubernetes ResourceQuota in the application namespace. |
| `quota_cpu_requests` | 8 | `""` | Total CPU requests allowed (e.g., `'4'`, `'4000m'`). |
| `quota_cpu_limits` | 8 | `""` | Total CPU limits allowed. |
| `quota_memory_requests` | 8 | `""` | Total memory requests. Must use binary suffixes (e.g., `'4Gi'`). |
| `quota_memory_limits` | 8 | `""` | Total memory limits. Must use binary suffixes. |
| `quota_max_pods` | 8 | `""` | Maximum pod count. |
| `quota_max_services` | 8 | `""` | Maximum Service count. |
| `quota_max_pvcs` | 8 | `""` | Maximum PVC count. Only relevant when `workload_type = 'StatefulSet'`. |

### B. Pod Disruption Budget & Topology

| Variable | Group | Default | Description |
|---|---|---|---|
| `enable_pod_disruption_budget` | 7 | `false` | Creates a PodDisruptionBudget. |
| `pdb_min_available` | 7 | `1` | Minimum pods available during disruptions. |
| `enable_topology_spread` | 7 | `false` | Distributes pods across failure domains. |
| `topology_spread_strict` | 7 | `false` | `true` = DoNotSchedule; `false` = ScheduleAnyway. |

### C. Health Probes & Uptime Monitoring

| Variable | Group | Default | Description |
|---|---|---|---|
| `startup_probe_config` | 14 | `{ enabled=true, path="/health", initial_delay_seconds=60, failure_threshold=6, ... }` | Startup probe. Container receives no traffic until this succeeds. |
| `health_check_config` | 14 | `{ enabled=true, path="/health", initial_delay_seconds=30, failure_threshold=3, ... }` | Liveness probe. |
| `uptime_check_config` | 14 | `{ enabled=true, path="/health" }` | Cloud Monitoring uptime check. |
| `alert_policies` | 14 | `[]` | Cloud Monitoring metric alert policies. |

---

## 6. Integrations

### A. Redis & Background Jobs

| Variable | Group | Default | Description |
|---|---|---|---|
| `enable_redis` | 21 | `false` | Enables Redis (bull-mq). When false, pg-boss is used. |
| `redis_host` | 21 | `""` | Redis server hostname or IP. Required when `enable_redis = true`. |
| `redis_port` | 21 | `'6379'` | Redis server TCP port (string). |
| `redis_auth` | 21 | `""` | Redis AUTH password. Sensitive. |

When `enable_redis = true`, a dedicated worker Deployment must be configured via `additional_services`.

### B. Environment Variables

`Twenty Common` injects the following environment variables automatically:

| Variable | Condition | Value |
|---|---|---|
| `MESSAGE_QUEUE_TYPE` | Always | `'pg-boss'` or `'bull-mq'` when `enable_redis = true` |
| `STORAGE_TYPE` | Always | `'local'` or `'s3'` when `enable_gcs_storage = true` |
| `REDIS_URL` | `enable_redis = true` | Constructed from `redis_host`, `redis_port`, `redis_auth` |
| `STORAGE_S3_NAME` | `enable_gcs_storage = true` | GCS bucket name |
| `STORAGE_S3_REGION` | `enable_gcs_storage = true` | Deployment region |
| `STORAGE_S3_ENDPOINT` | `enable_gcs_storage = true` | `https://storage.googleapis.com` |

User-supplied `environment_variables` are merged last and take precedence.

| Variable | Group | Default | Description |
|---|---|---|---|
| `environment_variables` | 5 | `{}` | Plain-text env vars. Set `SERVER_URL` and `FRONT_BASE_URL` here. |

### C. CI/CD

| Variable | Group | Default | Description |
|---|---|---|---|
| `enable_cicd_trigger` | 12 | `false` | Provisions a Cloud Build GitHub trigger. |
| `github_repository_url` | 12 | `""` | Full HTTPS URL of the GitHub repository. |
| `github_token` | 12 | `""` | GitHub PAT. Required on first apply. Sensitive. |
| `github_app_installation_id` | 12 | `""` | GitHub App installation ID. |
| `cicd_trigger_config` | 12 | `{ branch_pattern = "^main$" }` | Advanced Cloud Build trigger config. |
| `enable_cloud_deploy` | 12 | `false` | Provisions a Cloud Deploy pipeline. |
| `cloud_deploy_stages` | 12 | `[dev, staging, prod(approval)]` | Ordered Cloud Deploy stages. |

### D. Backup Import & Recovery

| Variable | Group | Default | Description |
|---|---|---|---|
| `backup_schedule` | 17 | `'0 2 * * *'` | Cron expression (UTC) for automated daily backups. |
| `backup_retention_days` | 17 | `7` | Days to retain backup files. |
| `enable_backup_import` | 17 | `false` | Triggers a one-time restore on apply. |
| `backup_source` | 17 | `'gcs'` | `'gcs'` or `'gdrive'`. |
| `backup_uri` | 17 | `""` | Full GCS URI or Google Drive file ID. |
| `backup_format` | 17 | `'sql'` | Backup format. Options: `sql`, `tar`, `gz`, `tgz`, `tar.gz`, `zip`. |

### E. Custom SQL Scripts

| Variable | Group | Default | Description |
|---|---|---|---|
| `enable_custom_sql_scripts` | 18 | `false` | Runs SQL scripts from GCS against the database after provisioning. |
| `custom_sql_scripts_bucket` | 18 | `""` | GCS bucket containing SQL scripts. |
| `custom_sql_scripts_path` | 18 | `""` | Path prefix within the bucket. |
| `custom_sql_scripts_use_root` | 18 | `false` | Run scripts as the root DB user. |

---

## 7. Platform-Managed Behaviours

| Behaviour | Implementation | Detail |
|---|---|---|
| **PostgreSQL 15 required** | `database_type = "POSTGRES_15"` default | Twenty only supports PostgreSQL. |
| **APP_SECRET auto-generated** | `random_password` in `Twenty Common` | 32-character alphanumeric secret. Stored in Secret Manager as `<resource_prefix>-app-secret`. |
| **Workload Identity access** | `App GKE` IAM bindings | GKE pods access secrets via Workload Identity — no SA keys mounted. |
| **pg-boss by default** | `MESSAGE_QUEUE_TYPE = "pg-boss"` | No Redis required by default. Set `enable_redis = true` to switch to bull-mq. |
| **STORAGE_TYPE defaults to local** | `STORAGE_TYPE = "local"` | File attachments are ephemeral unless `enable_gcs_storage = true`. |
| **StatefulSet auto-select** | `stateful_pvc_enabled = true` without explicit `workload_type` | Automatically resolves to `StatefulSet`. Setting `workload_type = "Deployment"` alongside `stateful_pvc_enabled = true` fails at plan time. |
| **Default db-init job** | Supplied by `Twenty Common` when `initialization_jobs = []` | PostgreSQL database and user created automatically. |
| **Scripts directory** | `scripts_dir = abspath("${module.twenty_app.path}/scripts")` | Init scripts sourced from `Twenty Common`. |
| **Memory quota binary suffix** | Validated in `App GKE` | `quota_memory_requests` and `quota_memory_limits` must use `Gi`/`Mi` suffixes. Bare integers cause pod scheduling failure. |

---

## 8. Variable Reference

All user-configurable variables exposed by `Twenty GKE`.

| Variable | Group | Default | Description |
|---|---|---|---|
| `module_description` | 0 | (Twenty GKE platform text) | Platform metadata. |
| `module_documentation` | 0 | `https://docs.radmodules.dev/docs/modules/Twenty_GKE` | Documentation URL. |
| `module_dependency` | 0 | `['Services GCP']` | Required modules. |
| `module_services` | 0 | (GCP service list) | GCP services consumed. |
| `credit_cost` | 0 | `150` | Deployment credit cost. |
| `require_credit_purchases` | 0 | `false` | Enforces credit balance check. |
| `enable_purge` | 0 | `true` | Permits full deletion on destroy. |
| `public_access` | 0 | `true` | Platform catalogue visibility. |
| `shared_users` | 0 | `[]` | Users with access regardless of `public_access`. Enforced by the platform. |
| `deployment_id` | 0 | `""` | Deployment ID suffix. Auto-generated if empty. |
| `resource_creator_identity` | 0 | (platform SA) | Service account for Terraform resource management. |
| `impersonation_service_account` | 0 | `""` | SA to impersonate for shell script API calls. |
| `project_id` | 1 | — | GCP project ID. **Required.** |
| `region` | 1 | `'us-central1'` | GCP region fallback. |
| `tenant_deployment_id` | 2 | `'demo'` | Short suffix for resource names. |
| `support_users` | 2 | `[]` | Email addresses for monitoring alerts. |
| `resource_labels` | 2 | `{}` | Labels applied to all resources. |
| `application_name` | 3 | `'twenty'` | Base resource name. Do not change after initial deployment. |
| `application_display_name` | 3 | `'Twenty CRM'` | Human-readable name. |
| `application_description` | 3 | `'Twenty CRM on GKE Autopilot'` | Deployment description. |
| `application_version` | 3 | `'latest'` | Container image tag. Pin for production. |
| `deploy_application` | 4 | `true` | Set `false` for infrastructure-only deployment. |
| `container_image_source` | 4 | `'custom'` | `'custom'` or `'prebuilt'`. |
| `container_image` | 4 | `""` | Container image URI. Leave empty for Cloud Build to manage. |
| `container_build_config` | 4 | `{ enabled=true }` | Cloud Build configuration. |
| `container_resources` | 4 | `{ cpu_limit="1000m", memory_limit="1Gi" }` | Pod resource limits. |
| `min_instance_count` | 4 | `1` | Minimum pod replicas. |
| `max_instance_count` | 4 | `3` | Maximum pod replicas. |
| `container_port` | 4 | `3000` | Twenty's native HTTP port. |
| `timeout_seconds` | 4 | `300` | Max backend response time. |
| `container_protocol` | 4 | `'http1'` | `'http1'` or `'h2c'`. |
| `enable_cloudsql_volume` | 4 | `true` | Injects Cloud SQL Auth Proxy sidecar. |
| `cloudsql_volume_mount_path` | 4 | `'/cloudsql'` | Auth Proxy Unix socket path. |
| `enable_image_mirroring` | 4 | `true` | Mirrors image into Artifact Registry. |
| `enable_vertical_pod_autoscaling` | 4 | `false` | Enables VPA. |
| `service_annotations` | 4 | `{}` | Kubernetes Service annotations. |
| `service_labels` | 4 | `{}` | Kubernetes Service labels. |
| `environment_variables` | 5 | `{}` | Plain-text env vars. Set `SERVER_URL` here. |
| `secret_environment_variables` | 5 | `{}` | Secret Manager references. |
| `secret_rotation_period` | 5 | `'2592000s'` | Rotation notification frequency. |
| `secret_propagation_delay` | 5 | `30` | Seconds to wait after secret creation. |
| `enable_auto_password_rotation` | 5 | `false` | Automated password rotation. |
| `rotation_propagation_delay_sec` | 5 | `90` | Seconds before pod restart after rotation. |
| `workload_type` | 6 | `null` | `'Deployment'` or `'StatefulSet'`. |
| `service_type` | 6 | `'LoadBalancer'` | Kubernetes Service type. |
| `namespace_name` | 6 | `""` | Kubernetes namespace. Auto-generated if empty. |
| `gke_cluster_name` | 6 | `""` | Target GKE cluster. Auto-discovered if empty. |
| `gke_cluster_selection_mode` | 6 | `'primary'` | Cluster selection strategy. |
| `prereq_gke_subnet_cidr` | 6 | `'10.201.0.0/24'` | CIDR for inline GKE subnet. |
| `session_affinity` | 6 | `'None'` | `'None'` or `'ClientIP'`. |
| `termination_grace_period_seconds` | 6 | `30` | Graceful termination period. |
| `enable_network_segmentation` | 6 | `false` | Creates Kubernetes NetworkPolicy. |
| `configure_service_mesh` | 6 | `false` | Enables Istio injection. |
| `enable_multi_cluster_service` | 6 | `false` | Creates ServiceExport for MCS. |
| `additional_services` | 6 | `[]` | Additional Kubernetes Deployments. |
| `deployment_timeout` | 6 | `1800` | Rollout wait timeout in seconds. |
| `enable_pod_disruption_budget` | 7 | `false` | Creates a PodDisruptionBudget. |
| `pdb_min_available` | 7 | `1` | Minimum pods during disruption. |
| `enable_topology_spread` | 7 | `false` | Pod topology spread constraints. |
| `topology_spread_strict` | 7 | `false` | Strict topology enforcement. |
| `enable_resource_quota` | 8 | `false` | Creates a Kubernetes ResourceQuota. |
| `quota_cpu_requests` | 8 | `""` | Total CPU requests quota. |
| `quota_cpu_limits` | 8 | `""` | Total CPU limits quota. |
| `quota_memory_requests` | 8 | `""` | Total memory requests quota. Binary suffix required. |
| `quota_memory_limits` | 8 | `""` | Total memory limits quota. Binary suffix required. |
| `quota_max_pods` | 8 | `""` | Maximum pod count. |
| `quota_max_services` | 8 | `""` | Maximum Service count. |
| `quota_max_pvcs` | 8 | `""` | Maximum PVC count. |
| `enable_nfs` | 13 | `false` | NFS storage. Not required for Twenty. |
| `nfs_mount_path` | 13 | `'/mnt/nfs'` | NFS mount path. |
| `nfs_volume_name` | 13 | `""` | NFS PVC name. Auto-generated if empty. |
| `nfs_instance_name` | 13 | `""` | Existing NFS GCE VM name. |
| `nfs_instance_base_name` | 13 | `'app-nfs'` | Base name for inline NFS VM. |
| `startup_probe_config` | 14 | `{ enabled=true, path="/health", initial_delay_seconds=60, ... }` | Startup probe. |
| `health_check_config` | 14 | `{ enabled=true, path="/health", initial_delay_seconds=30, ... }` | Liveness probe. |
| `uptime_check_config` | 14 | `{ enabled=true, path="/health" }` | Cloud Monitoring uptime check. |
| `alert_policies` | 14 | `[]` | Cloud Monitoring alert policies. |
| `database_type` | 15 | `'POSTGRES_15'` | Cloud SQL engine. Must be PostgreSQL. |
| `application_database_name` | 15 | `'twenty'` | Database name. Do not change after initial deployment. |
| `application_database_user` | 15 | `'twenty'` | Database user. |
| `database_password_length` | 15 | `32` | Password length. Range: 16–64. |
| `enable_postgres_extensions` | 15 | `false` | Enables PostgreSQL extension installation. |
| `postgres_extensions` | 15 | `[]` | PostgreSQL extensions to install. |
| `enable_mysql_plugins` | 15 | `false` | Not applicable to Twenty (PostgreSQL only). Platform compatibility. |
| `mysql_plugins` | 15 | `[]` | Not applicable to Twenty. |
| `sql_instance_name` | 15 | `""` | Existing Cloud SQL instance name. |
| `sql_instance_base_name` | 15 | `""` | Base name for new Cloud SQL instance. |
| `stateful_pvc_enabled` | 16 | `false` | Creates PVC and auto-selects StatefulSet. |
| `stateful_pvc_size` | 16 | `'10Gi'` | PVC size. |
| `stateful_pvc_mount_path` | 16 | `'/data'` | PVC mount path in container. |
| `stateful_pvc_storage_class` | 16 | `""` | StorageClass for PVC. |
| `stateful_headless_service` | 16 | `false` | Headless Service for StatefulSet DNS. |
| `stateful_pod_management_policy` | 16 | `'OrderedReady'` | StatefulSet pod management. |
| `stateful_update_strategy` | 16 | `'RollingUpdate'` | StatefulSet update strategy. |
| `stateful_fs_group` | 16 | `0` | fsGroup GID for PVC ownership. Set to the app's process GID. |
| `backup_schedule` | 17 | `'0 2 * * *'` | Automated backup cron (UTC). |
| `backup_retention_days` | 17 | `7` | Days to retain backups. |
| `enable_backup_import` | 17 | `false` | One-time restore on apply. |
| `backup_source` | 17 | `'gcs'` | `'gcs'` or `'gdrive'`. |
| `backup_uri` | 17 | `""` | GCS URI or Drive file ID for restore. |
| `backup_format` | 17 | `'sql'` | Backup format. |
| `enable_custom_sql_scripts` | 18 | `false` | Runs SQL scripts from GCS. |
| `custom_sql_scripts_bucket` | 18 | `""` | GCS bucket with SQL scripts. |
| `custom_sql_scripts_path` | 18 | `""` | Script path prefix. |
| `custom_sql_scripts_use_root` | 18 | `false` | Run as root DB user. |
| `enable_custom_domain` | 19 | `false` | Kubernetes Ingress for custom domains. |
| `application_domains` | 19 | `[]` | Custom domain names. |
| `reserve_static_ip` | 19 | `true` | Reserves a global static external IP. |
| `static_ip_name` | 19 | `""` | Static IP name. Auto-generated if empty. |
| `enable_cdn` | 19 | `false` | Cloud CDN on the HTTPS LB. |
| `network_tags` | 19 | `[]` | Network tags for GKE nodes/pods. |
| `network_name` | 19 | `""` | VPC network name. Auto-discovered if empty. |
| `enable_iap` | 20 | `false` | IAP on GKE backend service. |
| `iap_authorized_users` | 20 | `[]` | IAP-authorized users. |
| `iap_authorized_groups` | 20 | `[]` | IAP-authorized Google Groups. |
| `iap_oauth_client_id` | 20 | `""` | OAuth Client ID for IAP. |
| `iap_oauth_client_secret` | 20 | `""` | OAuth Client Secret for IAP. Sensitive. |
| `iap_support_email` | 20 | `""` | OAuth consent screen support email. |
| `enable_redis` | 21 | `false` | Redis (bull-mq). When false, pg-boss is used. |
| `redis_host` | 21 | `""` | Redis host. Required when `enable_redis = true`. |
| `redis_port` | 21 | `'6379'` | Redis port (string). |
| `redis_auth` | 21 | `""` | Redis password. Sensitive. |
| `enable_cloud_armor` | 21 | `false` | Cloud Armor policy on Ingress backend. |
| `admin_ip_ranges` | 21 | `[]` | Admin CIDR ranges for Cloud Armor. |
| `cloud_armor_policy_name` | 21 | `'default-waf-policy'` | Cloud Armor policy name. |
| `enable_vpc_sc` | 22 | `false` | VPC-SC perimeter enforcement. |
| `vpc_cidr_ranges` | 22 | `[]` | VPC CIDR ranges for VPC-SC access level. |
| `vpc_sc_dry_run` | 22 | `true` | Log VPC-SC violations without blocking. |
| `organization_id` | 22 | `""` | GCP Organization ID. **Must be set explicitly for VPC-SC.** |
| `enable_audit_logging` | 22 | `false` | Enables detailed Cloud Audit Logs. |

---

## 9. Outputs

| Output | Description |
|---|---|
| `service_name` | Name of the Kubernetes Service. |
| `service_url` | External URL of the Kubernetes LoadBalancer service. |
| `service_location` | GCP region of the GKE cluster. |
| `project_id` | GCP project ID. |
| `deployment_id` | Deployment ID suffix used in resource names. |
| `database_instance_name` | Name of the Cloud SQL PostgreSQL instance. |
| `database_name` | Name of the application database. |
| `database_user` | Name of the application database user. |
| `database_password_secret` | Secret Manager secret name for the database password. |
| `app_secret_name` | Secret Manager secret name for `APP_SECRET`. |
| `namespace` | Kubernetes namespace where the application is deployed. |
| `gke_cluster_name` | Name of the target GKE cluster. |
| `container_image` | Container image used for the deployment. |
| `static_ip_address` | Reserved static IP address (if `reserve_static_ip = true`). |

---

## 10. Configuration Pitfalls & Sensible Defaults

> Risk levels: **Critical** (data loss, full outage, security breach) — **High** (service unavailable or significant degradation) — **Medium** (degraded function or increased cost) — **Low** (minor impact).

| Variable | Sensible Default | Risk | Consequence of Incorrect Value |
|---|---|---|---|
| `SERVER_URL` (in `environment_variables`) | `""` | **Critical** | `Twenty Common` injects `SERVER_URL = ""` by default. Without a valid URL, all API calls return broken links, CORS errors block frontend requests, and OAuth integrations fail. Set `SERVER_URL` and `FRONT_BASE_URL` to the public URL after the LoadBalancer IP or custom domain is known. |
| `FRONT_BASE_URL` (in `environment_variables`) | `""` | **Critical** | Twenty uses `FRONT_BASE_URL` for all frontend redirects and email invitation links. An empty value breaks user invitations and notification emails. Must match `SERVER_URL`. |
| `APP_SECRET` (via `secret_environment_variables`) | Auto-generated once, stored in Secret Manager | **Critical** | `Twenty Common` generates `APP_SECRET` once at plan time. Rotating or deleting the secret invalidates all active JWT tokens, immediately logging out every user. Never manually rotate unless a full session invalidation is intentional. |
| `database_type` | `"POSTGRES_15"` | **Critical** | Twenty requires PostgreSQL. Changing to MySQL or NONE breaks Prisma schema migrations — the db-init job fails and the application will not start. |
| `application_database_name` | `"twenty"` | **Critical** | Changing after initial deployment orphans the existing database. Twenty will connect to a new empty database while the old data remains in Cloud SQL but unreferenced. |
| `application_database_user` | `"twenty"` | **Critical** | Changing after initial deployment creates a new user without permissions on existing database objects. All database operations will fail with permission errors. |
| `enable_cloudsql_volume` | `true` | **Critical** | Twenty connects to Cloud SQL via the Auth Proxy Unix socket. Disabling this removes the socket path, causing all database connections to fail on pod startup. |
| `enable_gcs_storage` | `false` | **High** | When `false`, file attachments are stored in the pod's ephemeral local filesystem and are permanently lost on pod restart or rolling update. Set to `true` for any production deployment where file retention is required. |
| `STORAGE_S3_ACCESS_KEY_ID` / `STORAGE_S3_SECRET_ACCESS_KEY` (in `secret_environment_variables`) | Not auto-generated | **High** | When `enable_gcs_storage = true`, the module sets `STORAGE_TYPE = "s3"` targeting GCS's S3-compatible endpoint, but HMAC keys are not generated automatically. Without them, all file upload and download operations fail with authentication errors. Generate HMAC keys in GCP Console. |
| `enable_redis` | `false` | **Medium** | Without Redis, Twenty uses `MESSAGE_QUEUE_TYPE = "pg-boss"` and `CACHE_STORAGE_TYPE = "memory"`. Memory caching is not shared between pods, causing cache inconsistency with multiple replicas. Enable Redis for any multi-replica GKE deployment. |
| `redis_host` | `""` | **High** | When `enable_redis = true` and `redis_host` is empty, the Redis URL is empty, causing Twenty to fail to connect. All background jobs stop processing immediately. |
| `additional_services` (worker config) | `[]` | **High** | When `enable_redis = true`, bull-mq is enabled but the main deployment does not run the worker consumer. Background jobs (email, webhooks, data sync) queue up but are never processed without a dedicated worker via `additional_services`. |
| `quota_memory_requests` / `quota_memory_limits` | `""` | **High** | When `enable_resource_quota = true`, these must use binary suffixes (e.g., `"4Gi"`, `"8192Mi"`). Bare integers are treated as bytes by Kubernetes, causing a quota-exceeded error that blocks all pod scheduling. A plan-time validation enforces the format. |
| `stateful_pvc_enabled` with `workload_type = "Deployment"` | — | **High** | This combination fails at plan time. Use `stateful_pvc_enabled = true` alone — it automatically resolves to `StatefulSet`. |
| `container_resources.memory_limit` | `"2Gi"` | **Medium** | Twenty's Node.js runtime and background processing require at least 2 GiB. Below this, OOM kills occur under load. Use `4Gi` for production workloads with large datasets. |
| `min_instance_count` | `1` | **Medium** | Setting to `0` with HPA allows scale-to-zero. Twenty cold starts take 30–60 seconds. For a CRM receiving webhooks or running scheduled jobs, ensure at least 1 replica is always running. |
| `session_affinity` | `"None"` | **Medium** | Twenty uses JWT-based authentication (stateless), so session affinity is not required for auth. However, if any in-memory caching is relied upon (Redis disabled), `"ClientIP"` affinity can reduce cache misses. |
| `enable_topology_spread` | `false` | **Medium** | Without topology spread, all Twenty pods may schedule on a single availability zone. A zone failure takes down the entire CRM. Enable for production. |
| `enable_pod_disruption_budget` | `false` | **Medium** | Without a PodDisruptionBudget, GKE cluster maintenance can evict all pods simultaneously. Enable when `max_instance_count > 1` to ensure availability during maintenance windows. |
| `organization_id` | `""` | **Medium** | VPC-SC perimeter is only activated when `organization_id` is explicitly set. `enable_vpc_sc = true` without this value has no effect. |
| `prereq_gke_subnet_cidr` | `"10.201.0.0/24"` | **High** | Each `App GKE` deployment sharing the same VPC must use a distinct CIDR. Overlapping with an existing subnet causes the GKE node pool provisioning to fail at apply time. |
| `enable_backup_import` | `false` | **High** | Setting to `true` triggers a database import on every `tofu apply`. If `backup_uri` is not a controlled restore snapshot, subsequent applies will overwrite live CRM data. |
| `vpc_sc_dry_run` | `true` | **Medium** | Dry-run mode logs VPC-SC violations without enforcing them. Switch to `false` only after auditing Cloud Logging to confirm no false positives. |
