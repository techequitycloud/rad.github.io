# Mautic on Google Cloud Run

This document provides a comprehensive reference for the `modules/Mautic_CloudRun` Terraform module. It covers architecture, IAM, configuration variables, Mautic-specific behaviours, and operational patterns for deploying Mautic on Google Cloud Run (v2).

---

## 1. Module Overview

Mautic is an open-source marketing automation platform used by 200,000+ organisations as a self-hosted alternative to HubSpot and Marketo. `Mautic CloudRun` is a **wrapper module** built on top of `App CloudRun`. It uses `App CloudRun` for all GCP infrastructure provisioning and injects Mautic-specific application configuration, database initialisation, and storage configuration via `Mautic Common`.

**Key Capabilities:**
*   **Compute**: Cloud Run v2 (Gen2), PHP/Apache container, 2 vCPU / 4 Gi by default. `min_instance_count = 1` recommended to avoid cold-start latency on campaign processing.
*   **Data Persistence**: Cloud SQL **MySQL 8.0** (not PostgreSQL). NFS (GCE VM or Filestore) for shared media files. GCS `mautic-media` bucket auto-provisioned by `Mautic Common`.
*   **Security**: Inherits Cloud Armor WAF, IAP, Binary Authorization, and VPC Service Controls from `App CloudRun`. `MAUTIC_ADMIN_PASSWORD` is auto-generated by `Mautic Common` and stored in Secret Manager.
*   **Caching**: Redis **enabled by default** (`enable_redis = true`) — configures Mautic's cache backend. When `redis_host` is empty and `enable_nfs = true`, the NFS server IP is used as the Redis host.
*   **CI/CD**: Cloud Build custom image pipeline by default; Cloud Deploy progressive delivery optional.
*   **Health Probes**: Startup probe uses TCP (not HTTP) to avoid Apache's HTTP→HTTPS redirect (301) blocking the probe. Liveness probe targets `/healthz`, a static file served without redirect.
*   **URL Injection**: `MAUTIC_SITE_URL` is automatically set to the predicted Cloud Run service URL. `HTTPS=on` is injected to prevent Apache redirect loops.

**Project & Application Identity**

| Variable | Group | Type | Default | Description |
|---|---|---|---|---|
| `project_id` | 1 | `string` | — | GCP project ID. **Required.** |
| `tenant_deployment_id` | 2 | `string` | `'demo'` | Short suffix appended to all resource names. |
| `support_users` | 2 | `list(string)` | `[]` | Email recipients for monitoring alerts. |
| `resource_labels` | 2 | `map(string)` | `{}` | Labels applied to all provisioned resources. |
| `application_name` | 3 | `string` | `'mautic'` | Base resource name. Do not change after initial deployment. |
| `application_display_name` | 3 | `string` | `'Mautic'` | Human-readable name shown in the GCP Console. |
| `application_description` | 3 | `string` | `'Mautic - Open-source marketing automation platform'` | Cloud Run service description. |
| `application_version` | 3 | `string` | `'5'` | Mautic image version tag. Increment to deploy a new release. |

**Wrapper architecture:** `Mautic CloudRun` calls `Mautic Common` to build an `application_config` object containing Mautic-specific environment variables, probe configuration, and the `db-init` job definition. `Mautic CloudRun` then overrides the startup probe to TCP and injects `MAUTIC_SITE_URL` and `HTTPS=on` into the merged config. `module_storage_buckets` carries the `mautic-media` bucket provisioned by `Mautic Common`. `scripts_dir` is resolved to `abspath("${module.mautic_app.path}/scripts")` at apply time.

**MySQL note:** Mautic requires **MySQL 8.0**. `database_type = "MYSQL_8_0"` is fixed by `Mautic Common`. PostgreSQL is not supported.

---

## 2. IAM & Access Control

`Mautic_CloudRun` delegates all IAM provisioning to `App_CloudRun`. The Cloud Run SA, Cloud Build SA, IAP service agent, and password rotation role sets are identical to those in [App_CloudRun §2](../App_CloudRun/App_CloudRun.md#2-iam--access-control).

**Auto-generated secrets:** `Mautic Common` generates one application-level secret: `MAUTIC_ADMIN_PASSWORD` (24 characters, no special characters). It is stored in Secret Manager and injected into the Cloud Run revision at startup. The `DB_PASSWORD` secret is provisioned automatically by `App CloudRun`.

**Database initialisation identity:** The `db-init` Cloud Run Job runs under the Cloud Run SA. It connects to Cloud SQL MySQL via the Auth Proxy Unix socket (since `enable_cloudsql_volume = true` by default), using the socket path under `/cloudsql`, `DB_USER`, and `ROOT_PASSWORD` (from Secret Manager).

For the complete role tables and IAP, password rotation, and public access details, see [App_CloudRun §2](../App_CloudRun/App_CloudRun.md#2-iam--access-control).

---

## 3. Core Service Configuration

### A. Compute (Cloud Run)

Mautic is a PHP/Apache application with significant startup overhead — it runs database migrations and PHP initialisation on first boot. `Mautic CloudRun` exposes `cpu_limit` and `memory_limit` as dedicated top-level variables.

**Cold start consideration:** `min_instance_count = 1` is the default (unlike Ghost which defaults to 0) because Mautic campaign processing and email queue jobs benefit from a warm instance. Cold starts can take 30–60 seconds due to PHP framework bootstrap and database migration checks.

| Variable | Group | Default | Description |
|---|---|---|---|
| `deploy_application` | 4 | `true` | Set `false` for infrastructure-only deployment (SQL, storage, secrets). |
| `container_image_source` | 4 | `'prebuilt'` | `'prebuilt'` deploys the official Mautic image. `'custom'` builds via Cloud Build using `Mautic Common`'s Dockerfile. |
| `container_image` | 4 | `""` | Override image URI. Leave empty for Cloud Build to manage. |
| `cpu_limit` | 4 | `'2000m'` | CPU per instance. 2 vCPU recommended for Mautic. |
| `memory_limit` | 4 | `'4Gi'` | Memory per instance. 4 Gi recommended; minimum 512 Mi. |
| `container_port` | 4 | `80` | Mautic/Apache listens on port 80. |
| `execution_environment` | 4 | `'gen2'` | Gen2 required for NFS mounts and GCS Fuse. |
| `timeout_seconds` | 4 | `300` | Max request duration. Increase for long-running campaign sends. |
| `min_instance_count` | 4 | `1` | Minimum instances. Set `1` to avoid cold-start on campaign processing. |
| `max_instance_count` | 4 | `3` | Maximum instances (cost ceiling). |
| `enable_cloudsql_volume` | 4 | `true` | Mautic connects via Unix socket. Set `false` for TCP. |
| `traffic_split` | 4 | `[]` | Percentage-based canary/blue-green traffic allocation. |
| `service_annotations` | 4 | `{}` | Advanced Cloud Run annotations. |
| `service_labels` | 4 | `{}` | Labels applied to the Cloud Run service. |

**Differences from `App CloudRun` defaults:**

| Variable | `App CloudRun` | `Mautic CloudRun` | Reason |
|---|---|---|---|
| `container_port` | `8080` | `80` | Mautic/Apache listens on port 80. |
| `cpu_limit` | `'1000m'` | `'2000m'` | Mautic's PHP campaign processing requires ≥2 vCPU. |
| `memory_limit` | `'512Mi'` | `'4Gi'` | Mautic's contact lists and campaign queues require significant RAM. |
| `min_instance_count` | `0` | `1` | Warm instance recommended for campaign processing. |
| `enable_image_mirroring` | `false` | `true` | Mautic mirrors its base image to Artifact Registry by default. |

### B. Database (Cloud SQL — MySQL 8.0)

Mautic requires **MySQL 8.0** — `Mautic Common` fixes `database_type = "MYSQL_8_0"`. PostgreSQL, SQL Server, and other engines are unsupported and will cause Mautic to fail at startup.

**Unix socket connection:** `enable_cloudsql_volume` defaults to `true`. `App CloudRun` injects the Auth Proxy sidecar and sets `DB_HOST` to the socket path under `/cloudsql`. The Mautic entrypoint script maps `DB_HOST` to `MAUTIC_DB_HOST`.

| Variable | Group | Default | Description |
|---|---|---|---|
| `database_type` | 12 | `'MYSQL_8_0'` | Fixed to MySQL 8.0. Do not change. |
| `application_database_name` | 12 | `'mautic'` | MySQL database name. **Do not change after initial deployment.** |
| `application_database_user` | 12 | `'mautic'` | MySQL application user. Password auto-generated and stored in Secret Manager. |
| `database_password_length` | 12 | `32` | Auto-generated password length. Range: 16–64. |
| `enable_auto_password_rotation` | 12 | `false` | Automated zero-downtime password rotation. |
| `rotation_propagation_delay_sec` | 12 | `90` | Seconds to wait after rotation before restarting the service. |

> `enable_postgres_extensions`, `enable_mysql_plugins`, `sql_instance_name`, and `sql_instance_base_name` are not exposed — database setup is managed by `Mautic Common`'s `db-init.sh` script.

### C. Storage (NFS & GCS)

**NFS is enabled by default** (`enable_nfs = true`). Mautic stores uploaded images, form assets, and other media on the NFS share so that all Cloud Run instances access a consistent filesystem. Requires `execution_environment = 'gen2'`.

**GCS media bucket:** `Mautic Common` automatically provisions a `mautic-media` GCS bucket in the deployment region. This bucket is separate from any buckets in `storage_buckets`.

| Variable | Group | Default | Description |
|---|---|---|---|
| `enable_nfs` | 11 | `true` | Provisions an NFS volume for shared media files. Requires `gen2`. |
| `nfs_mount_path` | 11 | `'/mnt/nfs'` | Container path where the NFS share is mounted. |
| `create_cloud_storage` | 11 | `true` | Set `false` to skip additional bucket creation. The `mautic-media` bucket from `Mautic Common` is always provisioned. |
| `storage_buckets` | 11 | `[{ name_suffix = "data" }]` | Additional GCS buckets beyond the auto-provisioned media bucket. |
| `gcs_volumes` | 11 | `[]` | GCS buckets to mount via GCS Fuse. Each entry: `name`, `bucket_name`, `mount_path`, `readonly`, `mount_options`. |
| `nfs_instance_name` | 11 | `""` | Name of an existing NFS GCE VM. Leave empty to auto-discover or use inline instance. |
| `nfs_instance_base_name` | 11 | `'app-nfs'` | Base name for an inline NFS GCE VM when none exists. |
| `manage_storage_kms_iam` | 11 | `false` | Creates a CMEK KMS keyring/key and enables CMEK on all storage buckets. |
| `enable_artifact_registry_cmek` | 11 | `false` | Creates an Artifact Registry KMS key and enables at-rest CMEK encryption of container images. |

### D. Networking

Cloud Run uses Direct VPC Egress to reach Cloud SQL's internal IP. Because `enable_cloudsql_volume = true` is the default, the Auth Proxy sidecar handles the Cloud SQL connection via Unix socket.

| Variable | Group | Default | Description |
|---|---|---|---|
| `ingress_settings` | 5 | `'all'` | `'all'` — public internet; `'internal'` — VPC only; `'internal-and-cloud-load-balancing'` — forces traffic through the HTTPS Load Balancer. |
| `vpc_egress_setting` | 5 | `'PRIVATE_RANGES_ONLY'` | `'PRIVATE_RANGES_ONLY'` routes only RFC 1918 traffic via VPC. `'ALL_TRAFFIC'` routes all egress via VPC. |

> `network_name` is not exposed. The module auto-discovers the `Services GCP` VPC network.

### E. Initialization & Bootstrap

A `db-init` Cloud Run Job is automatically provisioned by `Mautic Common` when `initialization_jobs` is left as the default empty list (`[]`). It uses the `mysql:8.0-debian` image and executes `Mautic_Common/scripts/db-init.sh`, which performs the following idempotent operations:

1. Connects to Cloud SQL MySQL via the Auth Proxy Unix socket.
2. Creates the Mautic database user with the password from Secret Manager.
3. Creates the Mautic database if it does not exist.
4. Grants the Mautic user full privileges on the database.

Override `initialization_jobs` with a non-empty list to replace this default with custom jobs.

**Startup behaviour:** `DOCKER_MAUTIC_RUN_MIGRATIONS=true` causes the Mautic container entrypoint to run database migrations on each startup. This is safe (idempotent) and ensures schema updates are applied automatically on version upgrades. The startup probe uses a TCP check with a 60-second initial delay and 20 failure threshold to accommodate this.

| Variable | Group | Default | Description |
|---|---|---|---|
| `initialization_jobs` | 13 | `[]` | One-shot Cloud Run Jobs. Leave empty for `Mautic Common` to supply the default `db-init` job. Non-empty list replaces it entirely. |
| `cron_jobs` | 13 | `[]` | Recurring jobs triggered by Cloud Scheduler. Critical for Mautic campaign processing. Each entry: `name`, `schedule` (cron UTC), `image`, `command`, `args`, `env_vars`, `secret_env_vars`, `cpu_limit`, `memory_limit`, `timeout_seconds`, `max_retries`, `paused`. |

### F. Mautic-Specific Environment Variables

`Mautic Common` and `Mautic CloudRun` inject the following environment variables automatically:

| Variable | Value | Source |
|---|---|---|
| `MAUTIC_DB_PORT` | `3306` | `Mautic Common` |
| `MAUTIC_ADMIN_EMAIL` | `var.mautic_admin_email` | `Mautic Common` |
| `MAUTIC_ADMIN_LOGIN` | `var.mautic_admin_username` | `Mautic Common` |
| `DOCKER_MAUTIC_ROLE` | `mautic_web` | `Mautic Common` |
| `DOCKER_MAUTIC_RUN_MIGRATIONS` | `true` | `Mautic Common` |
| `MAUTIC_TRUSTED_PROXIES` | `0.0.0.0/0` | `Mautic Common` |
| `MAUTIC_MAILER_FROM_NAME` | `var.mailer_from_name` | `Mautic Common` |
| `MAUTIC_MAILER_FROM_EMAIL` | `var.mailer_from_email` | `Mautic Common` |
| `MAUTIC_SITE_URL` | Predicted Cloud Run URL | `Mautic CloudRun` |
| `HTTPS` | `on` | `Mautic CloudRun` |
| `MAUTIC_ADMIN_PASSWORD` | Secret Manager reference | `App CloudRun` |
| `MAUTIC_DB_PASSWORD` | Secret Manager reference | `App CloudRun` |

**Mautic Application Settings:**

| Variable | Group | Default | Description |
|---|---|---|---|
| `mautic_admin_username` | 23 | `'admin'` | Mautic administrator username for initial setup. |
| `mautic_admin_email` | 23 | `'admin@example.com'` | Mautic administrator email. Used for login. |
| `mailer_from_name` | 23 | `'Mautic'` | Display name for outgoing campaign emails. |
| `mailer_from_email` | 23 | `'mautic@example.com'` | Sender address for outgoing campaign emails. Should match a verified sending domain. |

---

## 4. Advanced Security

### A. Cloud Armor WAF

Identical behaviour to `App CloudRun`. When `enable_cloud_armor = true`, a Global HTTPS Load Balancer with a Cloud Armor WAF policy (OWASP Top 10, adaptive DDoS, 500 req/min rate limiting) is provisioned in front of Cloud Run.

| Variable | Group | Default | Description |
|---|---|---|---|
| `enable_cloud_armor` | 10 | `false` | Provisions Global HTTPS LB + Cloud Armor WAF. Required for custom domains and DDoS protection. |
| `admin_ip_ranges` | 10 | `[]` | CIDR ranges exempted from WAF rules. |

### B. Identity-Aware Proxy (IAP)

When `enable_iap = true`, Cloud Run's native IAP integration is enabled directly on the service. Google identity authentication is required before users can access Mautic.

| Variable | Group | Default | Description |
|---|---|---|---|
| `enable_iap` | 5 | `false` | Enables IAP natively on the Cloud Run service. |
| `iap_authorized_users` | 5 | `[]` | Users/service accounts granted access. Format: `'user:email'`. |
| `iap_authorized_groups` | 5 | `[]` | Google Groups granted access. Format: `'group:name@example.com'`. |

### C. Binary Authorization

| Variable | Group | Default | Description |
|---|---|---|---|
| `enable_binary_authorization` | 8 | `false` | Enforces image attestation. Requires a Binary Authorization policy and attestor pre-configured in the project. |

### D. VPC Service Controls

| Variable | Group | Default | Description |
|---|---|---|---|
| `enable_vpc_sc` | 22 | `false` | Registers module API calls within the project's VPC-SC perimeter. A perimeter must already exist before enabling. |
| `vpc_cidr_ranges` | 22 | `[]` | VPC subnet CIDRs for the VPC-SC access level. |
| `vpc_sc_dry_run` | 22 | `true` | Log VPC-SC violations without blocking. Set `false` to enforce. |
| `organization_id` | 22 | `""` | GCP Organization ID. Must be set explicitly when the project is under a folder. |
| `enable_audit_logging` | 22 | `false` | Enable detailed Cloud Audit Logs. |

### E. Secret Manager Integration

Mautic application secrets are stored in Secret Manager and injected natively by Cloud Run at revision start.

| Variable | Group | Default | Description |
|---|---|---|---|
| `secret_environment_variables` | 6 | `{}` | Map of env var name → Secret Manager secret ID. Resolved at runtime. |
| `explicit_secret_values` | 6 | `{}` | Raw sensitive values written directly into Secret Manager at deploy time. |
| `secret_rotation_period` | 6 | `'2592000s'` | Frequency at which Secret Manager emits rotation notifications. Default: 30 days. |
| `secret_propagation_delay` | 6 | `30` | Seconds to wait after secret creation before dependent resources proceed. |

---

## 5. Traffic & Ingress

### A. HTTPS Load Balancer

When `enable_cloud_armor = true`, a Global HTTPS Load Balancer backed by a Serverless NEG is provisioned. Traffic flows: Internet → Cloud Armor → Global HTTPS LB → Serverless NEG → Cloud Run.

Setting `ingress_settings = 'internal-and-cloud-load-balancing'` forces all Mautic traffic through the LB, preventing direct `*.run.app` URL access.

### B. Cloud CDN

| Variable | Group | Default | Description |
|---|---|---|---|
| `enable_cdn` | 10 | `false` | Enables Cloud CDN on the HTTPS LB backend. Only effective when `enable_cloud_armor = true`. |
| `max_images_to_retain` | 10 | `7` | Maximum number of recent container images to keep in Artifact Registry. |
| `delete_untagged_images` | 10 | `true` | Automatically deletes untagged images from Artifact Registry. |
| `image_retention_days` | 10 | `30` | Days after which images are eligible for deletion. |

**Mautic consideration:** Mautic serves a mix of public landing pages, tracked email links, and authenticated marketing forms. CDN caching of form pages or personalised content may cause issues. Ensure `Cache-Control: no-cache` or `Vary` headers are set on form endpoints before enabling CDN.

### C. Custom Domains

| Variable | Group | Default | Description |
|---|---|---|---|
| `application_domains` | 10 | `[]` | Custom domain names for the HTTPS LB. Google-managed SSL certificates provisioned per domain. |

---

## 6. CI/CD & Delivery

### A. Cloud Build Triggers

| Variable | Group | Default | Description |
|---|---|---|---|
| `enable_cicd_trigger` | 8 | `false` | Provisions a Cloud Build GitHub trigger. |
| `github_repository_url` | 8 | `""` | Full HTTPS URL of the GitHub repository. |
| `github_token` | 8 | `""` | GitHub PAT. Required on first apply when `enable_cicd_trigger = true`. Sensitive. |
| `github_app_installation_id` | 8 | `""` | GitHub App installation ID (preferred for organisation repos). |
| `cicd_trigger_config` | 8 | `{ branch_pattern = "^main$" }` | Advanced trigger config. |

### B. Cloud Deploy Pipeline

| Variable | Group | Default | Description |
|---|---|---|---|
| `enable_cloud_deploy` | 8 | `false` | Provisions a Cloud Deploy pipeline. Requires `enable_cicd_trigger = true`. |
| `cloud_deploy_stages` | 8 | `[dev, staging, prod(approval)]` | Ordered promotion stages. Each: `name`, `require_approval`, `auto_promote`. |

---

## 7. Reliability & Scheduling

### A. Scaling & Concurrency

`min_instance_count = 1` is the default. Mautic's campaign processing benefits from a warm instance. `max_instance_count = 3` prevents runaway scaling. Both values are user-configurable (unlike Ghost which hardcodes them).

### B. Traffic Splitting

| Variable | Group | Default | Description |
|---|---|---|---|
| `traffic_split` | 4 | `[]` | Percentage-based traffic allocation across named revisions. All entries must sum to 100. |

### C. Health Probes & Uptime Monitoring

**Startup probe:** Uses TCP (port 80) rather than HTTP. Apache issues HTTP→HTTPS (301) redirects for all requests when `MAUTIC_SITE_URL` is an `https://` URL. Cloud Run health checks arrive via plain HTTP from `169.254.169.126` and are redirected, causing HTTP startup probes to never succeed. TCP probes check only that port 80 is open.

**Liveness probe:** Uses HTTP targeting `/healthz`. A static file is served at this path by Apache without triggering an HTTPS redirect, so the probe receives a 200 response over plain HTTP.

| Variable | Group | Default | Description |
|---|---|---|---|
| `startup_probe_config` | 14 | `{ type="TCP", initial_delay=60s, failure_threshold=20 }` | Startup probe. TCP probe avoids redirect failures. |
| `health_check_config` | 14 | `{ path="/healthz", initial_delay=120s }` | Liveness probe. HTTP to `/healthz`. |
| `uptime_check_config` | 14 | `{ enabled=true, path="/" }` | Cloud Monitoring uptime check. |
| `alert_policies` | 14 | `[]` | Cloud Monitoring metric alert policies. |

### D. Cron Jobs for Campaign Processing

Mautic requires scheduled commands for full functionality. Configure these via `cron_jobs`:

| Command | Purpose | Recommended Schedule |
|---|---|---|
| `mautic:campaigns:trigger` | Trigger scheduled campaign events | Every 15 minutes |
| `mautic:campaigns:messages` | Send queued campaign messages | Every 15 minutes |
| `mautic:queue:process` | Process email send queue | Every 5 minutes |
| `mautic:contacts:deduplicate` | Deduplicate contacts | Daily |
| `mautic:maintenance:cleanup` | Clean up old data | Weekly |

Example `cron_jobs` configuration:

```hcl
cron_jobs = [
  {
    name     = "campaign-trigger"
    schedule = "*/15 * * * *"
    command  = ["php", "/var/www/html/bin/console", "mautic:campaigns:trigger", "--env=prod"]
    cpu_limit    = "500m"
    memory_limit = "256Mi"
  },
  {
    name     = "queue-process"
    schedule = "*/5 * * * *"
    command  = ["php", "/var/www/html/bin/console", "mautic:queue:process", "--env=prod"]
    cpu_limit    = "500m"
    memory_limit = "256Mi"
  }
]
```

### E. Database Password Rotation

| Variable | Group | Default | Description |
|---|---|---|---|
| `enable_auto_password_rotation` | 12 | `false` | Deploys an automated password rotation job using Cloud Run and Eventarc. |
| `rotation_propagation_delay_sec` | 12 | `90` | Seconds to wait after rotation before restarting the service. |

---

## 8. Backup & Restore

### A. Automated Backups

`App CloudRun` provisions a scheduled Cloud Run backup job on the schedule defined by `backup_schedule`. Backup files are stored in the GCS backup bucket and subject to the `backup_retention_days` lifecycle rule.

| Variable | Group | Default | Description |
|---|---|---|---|
| `backup_schedule` | 7 | `'0 2 * * *'` | Cron schedule (UTC). Default: daily at 02:00 UTC. |
| `backup_retention_days` | 7 | `7` | Days to retain backups. |

### B. Custom SQL Scripts

| Variable | Group | Default | Description |
|---|---|---|---|
| `enable_custom_sql_scripts` | 9 | `false` | Run custom SQL scripts from GCS after database provisioning. |
| `custom_sql_scripts_bucket` | 9 | `""` | GCS bucket containing SQL scripts (no `gs://` prefix). |
| `custom_sql_scripts_path` | 9 | `""` | GCS prefix of SQL scripts to execute in lexicographic order. |
| `custom_sql_scripts_use_root` | 9 | `false` | Execute scripts as the MySQL root user. |

### C. Backup Import

| Variable | Group | Default | Description |
|---|---|---|---|
| `enable_backup_import` | 7 | `false` | Restore a backup file during deployment. |
| `backup_source` | 7 | `'gcs'` | `'gcs'` or `'gdrive'`. |
| `backup_uri` | 7 | `""` | GCS URI (e.g., `gs://bucket/mautic.sql`) or Google Drive file ID. |
| `backup_format` | 7 | `'sql'` | `'sql'`, `'tar'`, `'gz'`, `'tgz'`, `'tar.gz'`, or `'zip'`. |

---

## 9. Redis

| Variable | Group | Default | Description |
|---|---|---|---|
| `enable_redis` | 21 | `true` | Enable Redis as Mautic's caching backend. |
| `redis_host` | 21 | `""` | Redis host. Leave blank to use the NFS server IP when `enable_nfs = true`. |
| `redis_port` | 21 | `'6379'` | Redis port. |
| `redis_auth` | 21 | `""` | Redis auth password. Sensitive. Leave empty if Redis does not require authentication. |

**Redis dependency:** When `enable_redis = true` and `redis_host = ""`, the module uses the NFS server IP as the Redis host. This requires `enable_nfs = true`. If NFS is disabled, `redis_host` must be explicitly set.

---

## 10. Module Metadata

| Variable | Group | Default | Description |
|---|---|---|---|
| `deployment_id` | 0 | `""` | Short suffix appended to resource names. Auto-generated when empty. |
| `resource_creator_identity` | 0 | `"rad-module-creator@..."` | Service account used by Terraform to create resources. |
| `impersonation_service_account` | 0 | `""` | SA to impersonate for shell scripts. |
| `credit_cost` | 0 | `50` | Platform credits consumed per deployment. |
| `require_credit_purchases` | 0 | `false` | Enforce credit balance check before deploy. |
| `enable_purge` | 0 | `true` | Allow full resource deletion on destroy. |
| `public_access` | 0 | `false` | Make module visible in the public catalogue. |
| `shared_users` | 0 | `[]` | Users who can access regardless of `public_access`. Enforced by the platform. |

---

## Configuration Pitfalls & Sensible Defaults

> Risk levels: **Critical** (data loss, full outage, security breach) — **High** (service unavailable or significant degradation) — **Medium** (degraded function or increased cost) — **Low** (minor impact).

| Variable | Sensible Default | Risk | Consequence of Incorrect Value |
|---|---|---|---|
| `project_id` | _(required)_ | **Critical** | No default — deployment fails immediately without a valid project ID. |
| `database_type` | `"MYSQL_8_0"` | **Critical** | Mautic requires MySQL. Setting this to `POSTGRES` or `NONE` will break the application — Mautic's Doctrine ORM is not compatible with PostgreSQL in this module. |
| `enable_redis` | `true` | **High** | Defaults to `true`. Without Redis, Mautic falls back to file-based caching, which is incompatible with multi-instance deployments and causes session loss between requests. |
| `redis_host` | `""` (auto-resolves to NFS IP) | **High** | When `enable_redis = true` and `redis_host = ""`, the module uses the NFS server IP. If `enable_nfs = false`, there is no fallback — Mautic will fail to connect to Redis on startup. Set `redis_host` explicitly when NFS is disabled. |
| `enable_nfs` | `true` | **Critical** | Mautic stores uploaded media assets (images, attachments) on the NFS share. Without NFS, media uploads are lost on container restart and are not shared across multiple instances. |
| `min_instance_count` | `1` | **High** | Setting to `0` causes scale-to-zero. Mautic cron jobs rely on the container being reachable; cold starts also delay email delivery and campaign processing. Keep at `1` for production. |
| `cron_jobs` | `[]` (none configured) | **Critical** | Mautic campaigns, email queues, and lead scoring are entirely driven by cron. Without a configured cron job running `app/console mautic:segments:update` and `mautic:campaigns:trigger`, no campaigns fire, no emails are sent, and lead automation is completely non-functional. |
| `mautic_admin_email` | `"admin@example.com"` | **High** | The default email is a placeholder. Leaving it unchanged means system notifications and Mautic admin login recovery go to an unreachable address. |
| `mailer_from_email` | `"mautic@example.com"` | **High** | Mautic uses this as the `From` address for all outgoing campaign emails. The default is a placeholder — emails sent from this address will likely be rejected by receiving mail servers as the domain does not have SPF/DKIM configured. Set to a verified sending domain. |
| `ingress_settings` | `"all"` | **High** | The default `"all"` allows public internet access directly to the Cloud Run service URL. For production, use `"internal-and-cloud-load-balancing"` when fronted by Cloud Armor, or enable IAP to restrict admin access. |
| `enable_iap` | `false` | **Medium** | Mautic admin panel is publicly accessible when IAP is disabled and `ingress_settings = "all"`. Recommend enabling IAP or Cloud Armor for admin-facing deployments. |
| `enable_backup_import` | `false` | **Critical** | When set to `true`, `backup_uri` must be provided and accessible. An empty `backup_uri` with `enable_backup_import = true` will cause the deployment job to fail. |
| `backup_uri` | `""` | **Critical** | Required when `enable_backup_import = true`. An empty value with import enabled causes the restore job to error out during apply. |
| `execution_environment` | `"gen2"` | **High** | NFS mounts require gen2. Switching to `"gen1"` while `enable_nfs = true` will cause the Cloud Run service to fail to start with a volume mount error. |
| `memory_limit` | `"4Gi"` | **High** | Mautic is a PHP application with significant memory usage during campaign sends and contact imports. The described minimum is `512Mi` but production with large contact lists requires 2–4Gi. Too little memory causes PHP fatal errors and container OOM kills. |
| `cpu_limit` | `"2000m"` | **Medium** | Reducing below `1000m` significantly degrades Mautic's email processing and contact segment recalculation. |
| `timeout_seconds` | `300` | **Medium** | Mautic contact import and campaign send jobs can take longer than 300 seconds for large lists. Increase to `3600` for deployments that trigger long-running operations via HTTP. |
| `application_database_name` | `"mautic"` | **Critical** | Immutable after first deployment — changing this recreates the database resource and destroys all application data. |
| `application_database_user` | `"mautic"` | **Critical** | Immutable after first deployment — changing this recreates the user and invalidates stored credentials. |
| `database_password_length` | `32` | **Medium** | Do not reduce below 16 (enforced by validation). Short passwords increase brute-force risk for the database endpoint. |
| `backup_retention_days` | `7` | **Medium** | Seven days is insufficient for compliance workloads. Increase to 30 or 90 days for production. |
| `enable_cloud_armor` | `false` | **Medium** | Without Cloud Armor, Mautic is exposed to bot traffic and credential stuffing attacks on the login endpoint. Recommended for public-facing marketing automation deployments. |
| `vpc_egress_setting` | `"PRIVATE_RANGES_ONLY"` | **Medium** | Mautic must reach external SMTP and third-party APIs. `PRIVATE_RANGES_ONLY` routes only private traffic through VPC — public outbound traffic exits directly. If your firewall blocks direct public egress, change to `"ALL_TRAFFIC"`. |
| `enable_custom_sql_scripts` | `false` | **Medium** | Requires `custom_sql_scripts_bucket` and `custom_sql_scripts_path` to be set when enabled. Leaving them empty with the flag `true` causes the init job to fail silently. |
| `secret_propagation_delay` | `30` | **Low** | In multi-region projects, 30 seconds may be insufficient for Secret Manager replication. Increase to 60–90 seconds if deployments intermittently fail with "secret not found" errors. |
