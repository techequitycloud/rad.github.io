# Claude Code Instructions

## Git Workflow

- After pushing a fix branch, create a PR and immediately squash-merge it to main without asking for confirmation.
- Always develop on a `claude/...` feature branch, then PR → squash merge to main.
